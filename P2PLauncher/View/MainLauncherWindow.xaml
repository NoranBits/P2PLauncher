<Window x:Class="P2PLauncher.View.MainLauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:P2PLauncher.ViewModels"
        mc:Ignorable="d"
        Title="P2P Launcher" Height="420" Width="860"
        ResizeMode="NoResize">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Connection panel -->
        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Width="180" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="80" Text="{Binding Port}" Margin="8,0,0,0" />
            <Button Content="Connect" Command="{Binding ConnectCommand}" Margin="8,0,0,0" />
            <Button Content="Stop" Command="{Binding StopCommand}" Margin="8,0,0,0" />
        </StackPanel>

        <!-- Recent hosts -->
        <GroupBox Grid.Row="1" Grid.Column="0" Header="Recent Hosts" Margin="0,0,8,8">
            <ListBox ItemsSource="{Binding RecentHosts}" />
        </GroupBox>

        <!-- Status / logs placeholder -->
        <GroupBox Grid.Row="1" Grid.Column="1" Header="Status" Margin="8,0,0,8">
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap" />
        </GroupBox>

        <!-- Footer -->
        <StatusBar Grid.Row="2" Grid.ColumnSpan="2">
            <StatusBarItem Content="P2PLauncher .NET 9" />
        </StatusBar>
    </Grid>
</Window>
